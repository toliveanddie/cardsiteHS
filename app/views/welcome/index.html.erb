<h1>Welcome to a HS card app!</h1>
<h3>explore each hero in the drop down list above<br>
    List ALL CARDS? <%= link_to 'click here', cards_path, class: "btn btn-info" %>
    <% if user_signed_in? %>
        <% if current_user.profile(:user_id) == nil %>
            <hr>
            Please create profile <%= link_to 'here', new_profile_path, class: "btn btn-primary" %>
        <% end %>
    <% else %>
        <hr>
        <h2>New to the site? <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-primary btn-lg" %></h2>
    <% end %>
</h3>

<%= render 'index_partial' %>